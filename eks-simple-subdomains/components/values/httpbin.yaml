---
namespace: "httpbin"

image:
  tag: "latest"
  repository: "kennethreitz/httpbin"

deployment:
  containerPort: 80

service:
  port: 80

domain: "{{.nuon.install.sandbox.outputs.nuon_dns.public_domain.name}}"
