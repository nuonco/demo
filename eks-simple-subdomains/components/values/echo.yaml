---
namespace: "echo"

image:
  tag: "latest"
  repository: "jmalloc/echo-server"

deployment:
  containerPort: 8080

service:
  port: 80

domain: "{{.nuon.install.sandbox.outputs.nuon_dns.public_domain.name}}"
